@model Finn.MVC.Models.SendCodeViewModel
@{
    ViewBag.Title = "Проверка подлинности";
}

<h2>@ViewBag.Title</h2>

@using (Html.BeginForm("SendCode", "Account", new { ReturnUrl = Model.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" })) {
    @Html.AntiForgeryToken()
    @Html.Hidden("rememberMe", @Model.RememberMe)
    <h4>Двухфакторная аутентификация с помощью кода</h4>
    <hr />
    <div class="row">
        <div class="col-md-8">
            Выбор поставщика двухфакторной проверки подлинности:
            @Html.DropDownListFor(model => model.SelectedProvider, Model.Providers)
            <input type="submit" value="Продолжить" class="btn btn-success" />
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
